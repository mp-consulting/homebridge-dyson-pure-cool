# Types

TypeScript type definitions and ambient declarations.

## Structure

```
types/
└── homebridge-lib.d.ts   # Type definitions for homebridge-lib
```

## Files

### homebridge-lib.d.ts
Ambient type declarations for the `homebridge-lib` module, specifically for Eve HomeKit custom characteristics and services.

This provides TypeScript support for extended HomeKit functionality used by some services in the plugin.
